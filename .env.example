APP_ENV=local
APP_PORT=5173

# Host interface the published port binds to. Loopback by default so a server never answers
# the public interface directly beside its reverse proxy. Set to 0.0.0.0 only when something
# else on the network has to reach the container.
APP_BIND=127.0.0.1
